/*  

  This file contains the function of deletion of element from the array.
  
  Input Argument :- int Array , int number, int size 
  
  In this function we will delete the element from the array and return the new size of the array.
  
*/

int array_delete_function (int arr[], int size, int number)
{
   int i;
   for (int i =0; int size; i++)
   {
      if( arr[i] == number)
      break;
   }
   
   if( i == size)
   {
    return size;
   }
   
   for( int j =i; j<size ; j++)
   {
      arr[j] = a[j+1];
   }
   
   return size-1;
}
